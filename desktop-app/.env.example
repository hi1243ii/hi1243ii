# Base URL of the hosted EquipRent web app (the one with /api/* routes and
# the Postgres database). This gets baked into the static build at build time —
# rebuild the desktop app whenever this changes.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
